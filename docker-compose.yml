version: '3.8'

services:
  geovision-web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=web/app.py
      - FLASK_ENV=development
    command: python web/app.py
